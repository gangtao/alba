import * as React from "react";
import Page from "../../components/Page";

export default function Table({}) {
    const introduction = `we may need to build our own data table to meet the complex requirement for data analytic applications.`;

    return (
        <Page introduction={introduction}>
        </Page>
    )
}